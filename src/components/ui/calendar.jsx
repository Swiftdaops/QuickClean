// Calendar component removed — file intentionally blank.
// The component was deleted per project request.
// If you need it back, restore from git history or re-run the UI scaffold. "use client"; export default null;
